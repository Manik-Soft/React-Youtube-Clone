import React from "react";
import "./ChannelRow.css";
import { Avatar } from "@material-ui/core";

const ChannelRow = ({ image, channel, subs, noOfVideos, description }) => {
  return <div className="channelRow"></div>;
};

export default ChannelRow;
