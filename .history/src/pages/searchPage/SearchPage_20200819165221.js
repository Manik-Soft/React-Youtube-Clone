import React from "react";
import "./SearchPage.css";

const SearchPage = ({ toggle }) => {
  return <div className={toggle ? "searchPageBig" : "searchPage"}>hey</div>;
};

export default SearchPage;
